# Copy the relevant values into your shell environment; this file is not read
# automatically and must not contain credentials.
ROT_EVOL_ANALYSIS_ROOT=/path/to/analysis_results
ROT_EVOL_RESULT_ROOT=result
ROT_EVOL_JAXSPIN_DATA_DIR=/path/to/jaxspin/data
